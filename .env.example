MVIEW_AUTH_TOKEN=mobile-viewer-dev-token
ANDROID_ADB_SERVER_HOST=host.docker.internal
ANDROID_ADB_SERVER_PORT=5037
MVIEW_HOST=0.0.0.0
MVIEW_PORT=3000
# Set to true in Docker so scrcpy uses forward tunneling across the container boundary.
MVIEW_SCRCPY_TUNNEL_FORWARD=true
